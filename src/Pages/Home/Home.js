import React from "react";
import { Container } from "react-bootstrap";

const Home = () => {
	return (
		<Container fluid>
			<h4 className="text-center">Hello world</h4>
		</Container>
	);
};

export default Home;
